const Home = () => {
  /**
   * Build a table data factory.
   * Build a form to bring user input to build table data.
   * Build a table interface to do all primary action.
   * Build table/row/cell component.
   * Build menu to merge and unmerge.
   */
  return <h1>welcome to next.js!</h1>;
};

export default Home;
